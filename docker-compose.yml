version: '3.8'

services:
  app:
    build: .
    container_name: emareaimusic
    ports:
      - "8700:8700"
    volumes:
      - .:/app
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
